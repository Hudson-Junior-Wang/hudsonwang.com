export const name = "dsh-chat-pdf-export";

/**
 * The browser client owns the PDF export UI. This host half intentionally
 * exposes no Skill registration; the local Skill file is not required for the
 * installed button and is kept out of the package.
 */
export function apply() {}
